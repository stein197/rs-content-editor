import React from "react";
import {Container, Row, Col, Form, Button, Card, Alert} from "react-bootstrap";
import Fetch from "view/Fetch";

export default function Index(): JSX.Element {
	return (
		<Container>Hello from React!</Container>
	);
}
